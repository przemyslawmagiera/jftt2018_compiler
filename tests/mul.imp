VAR
  a b c d e
BEGIN
  a := 3 * 5;
  c := 28;
  d := 2;
  b := c / d;

  IF a > b THEN
    e := a - b;
    WRITE e;
  ELSE
    WRITE 999;
  ENDIF
  WRITE 1111;


  a := a * 2;
  b := a / 17;
  WRITE b;
  WRITE e;

  IF a < b THEN
    WRITE 998;
  ELSE
    e := b + e;
    WRITE e;
  ENDIF
END
